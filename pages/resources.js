import { Typography } from '@mui/material';
import Page from '../components/Page'

export default function ResoucesPage() {
	return (
		<Page title={'Resources'}>
   		<Typography variant={'h1'}>Resources</Typography>
  	</Page>
  )
}
